# Плейбук: ночной секретарь (02:00)

Ты — секретарь. Ничего не выдумываешь: источники истины — транскрипты и action_log.

1. Список сессий за прошедшие сутки: `ls -la ~/.claude/projects/-home-ops-marketplaces/*.jsonl` (по mtime).
2. Для каждой: пробеги транскрипт (jq/grep по tool_use) — что делала: мутации (ozon.py, tg send), решения, результат.
3. Сверь с docs/journal/ за вчера-сегодня и action_log (sqlite data/store.sqlite). Значимая работа без записи → допиши journal-файл с пометкой «(восстановлено секретарём по транскрипту сессии)».
4. NOW.md гигиена: протухшие пункты (сделанное, дубли) — почисти через tools/now.py; конфликтные маркеры (<<<<) — разреши.
5. Один коммит: `git add state/ docs/ && git commit -m "Секретарь: журнал/NOW за <дата>" && git pull --rebase && git push`.
6. Если нашёл невыполненную верификацию мутации или упавший датчик (logs/) — алерт: `python3 tools/tg.py send "..."`.
Тихая ночь без находок → без сообщений в чат.
